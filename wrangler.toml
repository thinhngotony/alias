name = "dotfiles-api"
main = "worker.js"
compatibility_date = "2024-01-01"

# Custom domain route (configure in Cloudflare dashboard after deploy)
# routes = [
#   { pattern = "api.hyberorbit.com/*", zone_name = "hyberorbit.com" }
# ]
